# tinyqwen 运行时配置（key = value，'#' 为注释）
# 用法：tinyqwen --config tinyqwen.conf ...
# 优先级：CLI 开关 > 本文件 > 默认值。

# matvec kernel 实现：
#   ref             标量参考（double 累加，默认，正确性基准 + 兜底）
#   double_2_float  内层累加 double→float 的变体（~9% 提速，误差略大）
# 以后新增优化版会在这里可选，如 neon / neon_mt / i8。
matvec_impl = ref
